# 팀 전달용 정리

## 1. 현재까지 구현된 것

- **monitor_main.py**
  - PyQt6 기반 모니터링 앱
  - MQTT 구독자로 라즈베리에서 오는 센서 데이터를 수신
  - 수신 데이터는 `stress_data.db`의 `sensor_log` 테이블에 저장
  - 서버 상태(`/latest`)를 `MonitoringServerState`로 갱신
  - 실행 시 HTTP 서버도 함께 시작

- **monitor_server.py**
  - 경량 HTTP 서버
  - 엔드포인트:
    - `/health` → 헬스체크
    - `/latest` → 최신 수신 데이터 JSON
    - `/api/history` → DB 최근 데이터 목록 JSON
    - `/dashboard` → HTML 대시보드
      - `?lang=ko`로 한국어 대시보드 지원
  - 서버 시작 시 DB의 최신 레코드를 읽어 `/latest` 초기화함

- **monitor_config.json**
  - MQTT 브로커 설정
  - 서버 호스트/포트 설정
  - `use_dummy_data` 옵션으로 더미 데이터 모드 전환 가능

## 2. 라즈베리팀에 줄 파일

- `monitor_main.py`
- `monitor_server.py`
- `mqtt_publisher_example.py`
- `monitor_config.json`
- `requirements.txt`
- `README_raspberry.md`
- `service_instructions.md`
- `stress_data.db` (이미 생성된 경우 포함하면 테스트용 데이터 확인 가능)
- `test_monitor_server.py` / `verify_end_to_end.py` / `e2e_test.py` (검증용)

## 3. 라즈베리팀에게 전달할 내용

- **MQTT 토픽**: `sensor/stress_data`
- **JSON 스키마**:
  - `device_id`, `timestamp`, `heart_rate`, `spo2`, `stress_level`
- **권장 실행 방법**:
  1. `python -m venv .venv`
  2. Windows: `Activate.ps1` 또는 Linux/Mac: `source .venv/bin/activate`
  3. `pip install -r requirements.txt`
- **서버 실행**:
  - `python monitor_main.py` (GUI + MQTT + HTTP 서버 통합 실행)
  - 또는 `python monitor_server.py` (서버만 실행)

## 4. 테스트/검증 방법

- **서버 정상 여부**: [http://127.0.0.1:8765/health](http://127.0.0.1:8765/health)
- **데이터 조회**:
  - [http://127.0.0.1:8765/latest](http://127.0.0.1:8765/latest)
  - [http://127.0.0.1:8765/api/history?limit=5](http://127.0.0.1:8765/api/history?limit=5)
- **대시보드**:
  - [http://127.0.0.1:8765/dashboard](http://127.0.0.1:8765/dashboard)
  - [http://127.0.0.1:8765/dashboard?lang=ko](http://127.0.0.1:8765/dashboard?lang=ko)

## 5. 배포 안정화 안내

- `service_instructions.md`에 Windows용 NSSM 기반 자동 재시작 설정 방법 정리함
- 필요하면 해당 문서대로 서비스 등록하면 서버가 재시작되도록 구성 가능

## 6. 현재 상태

- 프로젝트는 실행 가능한 상태
- 서버는 `8765`번 포트에서 동작함
- `monitor_main.py`에서 MQTT 데이터 수신 및 저장이 확인됨
- `README_raspberry.md`와 `service_instructions.md`까지 작성 완료
